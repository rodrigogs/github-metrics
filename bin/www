#!/usr/bin/env node

const logger = require('../src/config/logger');

const app = require('../src/app');

app()
  .then((instance) => {
    logger.info('Application running on port', instance.server.address().port);
  })
  .catch((err) => {
    logger.error('Application crashed:', err);
    process.exit(1);
  });
